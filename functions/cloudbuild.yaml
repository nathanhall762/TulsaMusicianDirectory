# cloudbuild for cloud functions

steps:
  # install
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']

  # build
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'build']

  # deploy
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'deploy']
