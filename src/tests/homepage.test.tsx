import { describe, it, expect } from 'vitest';
// import { render } from '@testing-library/react';
// import HomePage from '../components/homepage';

// Tests
describe('Renders homepage correctly', async () => {
  it('Should render the page correctly', async () => {
    // this is broken because of context but we're moving away from that anyways

    // render(<HomePage />);

    // const approveMusician = screen.findByText('Approve Musician');

    expect('fake value').not.toBeNull();
  });
});
